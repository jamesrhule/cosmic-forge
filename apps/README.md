# `apps/`

Reserved for future React/Next applications managed by the pnpm
workspace (`pnpm-workspace.yaml`). The legacy fixture-backed
TanStack frontend stays at the repo root and is not part of this
directory.
