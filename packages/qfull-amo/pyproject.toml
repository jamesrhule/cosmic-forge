# Placeholder package — created during Phase 0 monorepo scaffolding
# (commit ucglef1-v1.0.0). The real implementation lands later. The
# uv workspace at the repo root iterates packages/* and requires every
# member to expose a pyproject.toml; this stub satisfies that contract
# without publishing any code.

[project]
name = "qfull-amo"
version = "0.0.0"
description = "QFull-AMO — atomic, molecular & optical physics (cold-atom dynamics, photoionisation, strong-field)."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# No package source yet; build is intentionally empty.
bypass-selection = true
